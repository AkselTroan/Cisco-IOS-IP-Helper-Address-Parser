# Progress
### Current working features
* Parse and store Switch Virtual Interfaces
* Parse and store the ip helper-addresses for each given Switch Virtual Interface
* Compare the running configuration to a given template
* Notify the user which interfaces which are missing. Additionally notify missing ip helper-addresses
* Check a list of config files and check them all at once and comparing it to one template config

### Missing features
* How to store the data in a efficient way which makes it easy to understand what is missing where (Partially implemented)
* Suggest that one running config address it going to removed in the future. Store (Has Address that should be removed)
* Supply a new configuration with corrected config(?)


